services:
  mysql:
    image: elisasc/clima-db
    container_name: clima_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: parde4
      MYSQL_DATABASE: clima_db
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: